#include <stdio.h>
/**
 * main - Entry point.
 * Return: 0
 */

int main(void)
{
	int a = 0;
	int b = 98;

	for ( ; a <= '9'; a++)
		putchar(a);

	for ( ; b <= 102 ; b++)
		putchar(b);
	putchar(10);
	return (0);
}
